<% content_for :title, "New additional service" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New additional service</h1>

  <%= render "form", additional_service: @additional_service %>

  <%= link_to "Back to additional services", additional_services_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
